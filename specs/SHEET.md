# Sheet — One pager

## Acceptance

Given `sheet.json` for a pack  
When Pack → Sheet tab renders  
Then claim + blocks show with links to sessions/concepts  
And revision flow diagram is present

## Tests

- `tests/unit/sheet.render.test.ts`
