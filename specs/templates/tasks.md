# Tasks NNN — <feature name>

> The plan broken into commit-sized steps, in dependency order. Update the
> checkboxes as you land them; the file merges with the feature as its record.

**Plan:** ./plan.md

- [ ] 1. … *(gates first: linter/type/whitelist groundwork before content)*
- [ ] 2. …
- [ ] 3. Tests: …
- [ ] 4. Specs: update specs/<domain>.md invariants …
- [ ] 5. Verify offline: `npm test` + `npm run smoke` + contrast (if colors)
- [ ] 6. Ship via /ship (PR references `specs/NNN-<slug>/`)
